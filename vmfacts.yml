---
- hosts: all
  gather_facts: false
  connection: local
  vars_files:
  - "/var/lib/awx/keys/vcenter.yml"
  tasks:
  - name: testing
    vmware_guest_facts:
      hostname: "{{ vcenter_host }}"
      username: "{{ vcenter_user }}"
      password: "{{ vcenter_pass }}"
      datacenter: "{{ datacenter }}"
      folder: "{{ datacenter }}/vm/{{ vmfolder }}"
      name: "{{ inventory_hostname }}"
      validate_certs: no
    register: facts

  - name: showit
    debug:
      msg: "{{ facts }}"
